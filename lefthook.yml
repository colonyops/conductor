pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run lint
    format:
      run: bun run format && git diff --exit-code -- src/ tests/
    typecheck:
      run: bun run typecheck

pre-push:
  commands:
    test:
      run: bun run test
