{
  "name": "conductor",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "conductor": "bun src/index.ts",
    "start": "bun src/index.ts start",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/ tests/",
    "format": "biome format --write src/ tests/",
    "test": "bun test tests/unit/",
    "test:watch": "bun test --watch tests/unit/",
    "test:integration": "bun test tests/integration/scenarios/ --timeout=120000",
    "test:e2e": "bun test tests/e2e/",
    "build": "bun build src/index.ts --outfile dist/conductor.js",
    "prepare": "git config --local core.hooksPath .git/hooks && lefthook install --force"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "lefthook": "^2.1.9",
    "typescript": "^5.7.3"
  }
}
